apiVersion: v1
kind: Service
metadata:
  name: hospital-frontend
  namespace: num
spec:
  type: NodePort
  selector:
    app: hospital-frontend
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
    nodePort: 31001